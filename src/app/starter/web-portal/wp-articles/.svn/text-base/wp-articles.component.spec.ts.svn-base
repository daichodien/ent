import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WPArticlesComponent } from './wp-articles.component';

describe('WpArticlesComponent', () => {
  let component: WPArticlesComponent;
  let fixture: ComponentFixture<WPArticlesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WPArticlesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WPArticlesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
