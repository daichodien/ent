import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VerifyTripRecordComponent } from './verify-trip-record.component';

describe('VerifyTripRecordComponent', () => {
  let component: VerifyTripRecordComponent;
  let fixture: ComponentFixture<VerifyTripRecordComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VerifyTripRecordComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VerifyTripRecordComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
