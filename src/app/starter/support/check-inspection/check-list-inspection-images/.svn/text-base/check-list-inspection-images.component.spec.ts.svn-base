import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CheckListInspectionImagesComponent } from './check-list-inspection-images.component';

describe('CheckListInspectionImagesComponent', () => {
  let component: CheckListInspectionImagesComponent;
  let fixture: ComponentFixture<CheckListInspectionImagesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CheckListInspectionImagesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CheckListInspectionImagesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
