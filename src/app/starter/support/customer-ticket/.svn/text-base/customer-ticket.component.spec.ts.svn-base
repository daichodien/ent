import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FleetIncidentComponent } from './fleet-incident.component';

describe('FleetMaintenanceComponent', () => {
  let component: FleetIncidentComponent;
  let fixture: ComponentFixture<FleetIncidentComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FleetIncidentComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FleetIncidentComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
