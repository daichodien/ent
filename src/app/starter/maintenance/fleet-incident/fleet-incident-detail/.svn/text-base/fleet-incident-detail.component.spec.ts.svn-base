import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FleetIncidentDetailComponent } from './fleet-incident-detail.component';

describe('FleetIncidentDetailComponent', () => {
  let component: FleetIncidentDetailComponent;
  let fixture: ComponentFixture<FleetIncidentDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FleetIncidentDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FleetIncidentDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
