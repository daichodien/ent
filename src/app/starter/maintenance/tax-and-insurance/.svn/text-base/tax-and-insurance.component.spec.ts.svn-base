import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TaxAndInsuranceComponent } from './tax-and-insurance.component';

describe('TaxAndInsuranceComponent', () => {
  let component: TaxAndInsuranceComponent;
  let fixture: ComponentFixture<TaxAndInsuranceComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TaxAndInsuranceComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TaxAndInsuranceComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
