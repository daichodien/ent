import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FleetMaintenanceDetailComponent } from './fleet-maintenance-detail.component';

describe('FleetMaintenanceDetailComponent', () => {
  let component: FleetMaintenanceDetailComponent;
  let fixture: ComponentFixture<FleetMaintenanceDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FleetMaintenanceDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FleetMaintenanceDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
