import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TripRecordComponent } from './trip-record.component';

describe('TripRecordComponent', () => {
  let component: TripRecordComponent;
  let fixture: ComponentFixture<TripRecordComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TripRecordComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TripRecordComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
