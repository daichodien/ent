import { DateTimeFormatDirective } from './date-time-format.directive';

describe('DateTimeFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
